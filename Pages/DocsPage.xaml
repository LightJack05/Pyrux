<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<Page
    xmlns:docsPages="using:Pyrux.Pages.DocsPages"
    x:Class="Pyrux.Pages.DocsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pyrux.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    NavigationCacheMode="Required">

    <Grid>
        <Border Background="{ThemeResource SystemChromeLowColor}" CornerRadius="10" Margin="10">
            <Grid>
                <Frame Grid.Row="1" SourcePageType="docsPages:DocsPageWrapper"/>
                <Button Grid.Row="0" x:Name="btnPopoutDocs" Click="btnPopoutDocs_Click" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8" CornerRadius="10" ToolTipService.ToolTip="Popout">
                    <SymbolIcon Symbol="NewWindow"/>
                </Button>
            </Grid>
            
        </Border>
    </Grid>
</Page>
