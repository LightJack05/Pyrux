<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Pyrux.Pages.SettingsPages.TutorialSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pyrux.Pages.SettingsPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    NavigationCacheMode="Required"
    Loaded="Page_Loaded">

    <StackPanel Orientation="Vertical">
        <Border Background="{ThemeResource SystemChromeMediumHighColor}" CornerRadius="10" Margin="10">
            <Grid Margin="10" HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Show tutorial" VerticalAlignment="Center"/>
                <ToggleSwitch Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center"  x:Name="tswShowTutorial" Toggled="tswShowTutorial_Toggled"/>
            </Grid>
        </Border>
    </StackPanel>
</Page>
